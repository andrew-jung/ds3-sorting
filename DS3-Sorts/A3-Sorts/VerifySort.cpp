
/*
#include <vector>
#include <algorithm>

Insert into perform sort, create vector copies of arrays and compare
int* arrayCopy2 = generateCopy();
std::vector<int> vectorCopy(arrayCopy2, arrayCopy2 + size);
sort(vectorCopy.begin(), vectorCopy.end());

bool flag = true;
for (int i = 0; i < size; i++)
{
	if (sortedArray[i] != vectorCopy[i])
	{
		flag = false; // mismatch occured
		break;
	}
}
if (flag == true) -> Sorting is 'correct'.

*/